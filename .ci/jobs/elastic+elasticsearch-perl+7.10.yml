---
- job:
    name: elastic+elasticsearch-perl+7.10
    display-name: 'elastic / elasticsearch-perl # 7.10'
    description: Testing the elasticsearch-perl 7.10 branch.
    parameters:
    - string:
        name: branch_specifier
        default: refs/heads/7.10
        description: the Git branch specifier to build (&lt;branchName&gt;, &lt;tagName&gt;,
          &lt;commitId&gt;, etc.)
    triggers:
    - github
    - timed: 'H */12 * * *'
